import { FaJava, FaPython, FaReact } from "react-icons/fa";

const Skills = () => (
  <section>
    <h2>Skills</h2>
    <div>
      <FaJava size={50} />
      <FaPython size={50} />
      <FaReact size={50} />
      {/* Add more skills */}
    </div>
  </section>
);

export default Skills;
